# aiohttp is imported directly by bridge.py (OpenAICompatibleRun's SSE
# streaming) as well as being a transitive dependency of discord.py — don't
# remove it thinking it's unused just because bridge.py's own imports are
# what changed most recently.
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
attrs==26.1.0
discord.py==2.7.1
frozenlist==1.8.0
idna==3.19
multidict==6.8.0
propcache==0.5.2
typing_extensions==4.16.0
yarl==1.24.5
